// Compile a mono application
task buildMono << {
   exec {
      executable "nuget"
      workingDir "${projectDir}"
      args=[
         "restore"
      ]
   }

   exec {
      executable "xbuild"
      args=[
         "/p:Configuration=Release"
      ]
   }
}
